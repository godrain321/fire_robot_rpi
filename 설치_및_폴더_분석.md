# fire_robot_rpi 설치 및 폴더 분석

## 시스템과 설치 결과

- 장치: Raspberry Pi 5 Model B Rev 1.1
- 운영체제: Ubuntu 24.04.4 LTS ARM64
- 프로젝트 위치: `/home/gosunwoo/fire_robot_rpi`
- ROS 2 Jazzy Desktop, RViz, colcon, rosdep, OpenCV 및 개발 도구 설치 완료
- Raspberry Pi용 `libcamera 0.7.1` 빌드 완료
- `camera_ros 0.7.0` Release 빌드 완료
- `sllidar_ros2 1.0.1` 빌드 완료
- MLX90640 C++ 실행 파일 및 Python 공유 라이브러리 빌드 완료
- DFRobot C4001 Python 코드와 의존성 검증 완료
- 사용자 `gosunwoo`를 `video`, `dialout`, `i2c` 그룹에 추가

설치 자동화 스크립트는 프로젝트 최상위의
`install_ubuntu2404_dependencies.sh` 파일이다.

메인 저장소에는 `.gitmodules`가 빠져 있었다. 따라서 기본 clone만 하면
`camera_ros`, `libcamera`, `sllidar_ros2`, `CImg`, `DFRobot_C4001` 폴더가
비어 있었다. 메인 저장소에 기록된 Git 커밋 ID를 기준으로 각 원본
저장소를 찾아 지정 버전으로 복원했다.

## 재부팅

장치 그룹 권한과 설치 과정에서 갱신된 커널 초기화 파일을 완전히
적용하려면 다음 명령으로 한 번 재부팅한다.

```bash
sudo reboot
```

## camera_ws

ROS 2 카메라 워크스페이스다.

### `camera_ws/src/libcamera`

- Raspberry Pi 카메라 처리 계층
- Pi 5의 PiSP와 이전 Raspberry Pi의 VC4 파이프라인 포함
- IMX708, IMX219, IMX477, OV5647 등 센서 튜닝 파일 포함
- 현재 Pi 5용 `rpi/pisp`, `rpi/vc4` 파이프라인이 활성화됨
- Python 바인딩과 V4L2 호환 계층도 함께 빌드됨

### `camera_ws/src/camera_ros`

- libcamera가 받은 영상을 ROS 2 이미지 메시지로 변환
- `/camera/image_raw`, `/camera/camera_info` 등의 토픽 발행
- 독립 실행 노드와 composable component 제공

### `camera_ws/build`, `camera_ws/install`, `camera_ws/log`

colcon이 생성한 빌드 결과, 설치 결과 및 로그다. 실행 전에
`camera_ws/install/setup.bash`를 source해야 한다.

실제 연결된 `IMX708 Wide` 카메라를 감지했고, 640x480 XRGB8888 스트림
구성과 프레임 처리가 정상 동작하는 것을 확인했다.

카메라 실행:

```bash
cd ~/fire_robot_rpi
source /opt/ros/jazzy/setup.bash
source camera_ws/install/setup.bash

ros2 run camera_ros camera_node --ros-args \
  -p width:=640 \
  -p height:=480
```

다른 터미널에서 영상 보기:

```bash
source /opt/ros/jazzy/setup.bash
source ~/fire_robot_rpi/camera_ws/install/setup.bash
ros2 run image_view image_view --ros-args --remap image:=/camera/image_raw
```

카메라 캘리브레이션 YAML이 없다는 경고는 일반 촬영에는 문제가 없다.
거리 계산이나 렌즈 왜곡 보정이 필요할 때 별도 캘리브레이션을 수행한다.

## inno_jazzy_ws

LiDAR, RF2O 오도메트리, SLAM Toolbox와 센서 bringup을 통합한 메인 ROS 2 Jazzy 워크스페이스다.

### `inno_jazzy_ws/src/sllidar_ros2`

- A1, A2, A3, C1, S1, S2, S3, T1 모델 지원
- USB 시리얼 및 TCP 연결 지원
- 측정값을 `sensor_msgs/msg/LaserScan`으로 발행
- 모델별 launch 파일과 RViz 설정 포함
- 빌드된 실행 파일: `sllidar_node`, `sllidar_client`

A1 모델 실행 예:

```bash
cd ~/fire_robot_rpi
source /opt/ros/jazzy/setup.bash
source inno_jazzy_ws/install/setup.bash
ros2 launch sllidar_ros2 view_sllidar_a1_launch.py
```

LiDAR는 `/dev/ttyUSB0`에서 통신, 스캔, SLAM 및 지도 저장까지 확인했다.
연결 장치는 다음 명령으로 확인한다.

```bash
ls -l /dev/ttyUSB*
```

예전의 별도 `inno_ws`는 동일한 `sllidar_ros2` 중복본이어서 이 작업공간으로 통합했다.

## mlx90640

MLX90640 32x24 열화상 센서 데모다.

### `mlx90640/demo codes/mlx90640/cpp`

- CImg와 X11을 사용해 열화상 창을 표시
- ARM64 실행 파일 `main` 빌드 완료

실행:

```bash
cd ~/fire_robot_rpi/'mlx90640/demo codes/mlx90640/cpp'
./main
```

### `mlx90640/demo codes/mlx90640/python`

- `ctypes`로 C++ 공유 라이브러리를 호출
- NumPy, Pillow, Tkinter로 열화상 표시
- `python/lib/libmlx90640.so` ARM64 빌드 완료

실행:

```bash
cd ~/fire_robot_rpi/'mlx90640/demo codes/mlx90640/python'
python3 mlx90640.py
```

I2C 버스는 활성화됐지만 검사 당시 기본 주소 `0x33`에서 센서가 발견되지
않았다. MLX90640의 전원, SDA, SCL, GND 배선을 확인하고 다음 명령으로
다시 검사한다.

```bash
i2cdetect -y 1
```

정상 연결되면 `30:` 행의 `3` 열에 `33`이 나타난다.

## mmwave

DFRobot C4001 24GHz mmWave 인체 감지 센서 라이브러리다.

- Arduino 라이브러리와 예제
- MicroPython 구현
- Raspberry Pi Python 구현
- 존재 감지 예제
- 거리와 속도 측정 예제

Python 의존성 `smbus`, `pyserial`, `RPi.GPIO`는 설치 및 import 검증을
완료했다.

존재 감지 실행:

```bash
cd ~/fire_robot_rpi/mmwave/DFRobot_C4001/python/raspberrypi/examples
python3 motion_detection.py
```

거리와 속도 측정:

```bash
python3 motion_range_velocity.py
```

검사 당시 C4001로 확인되는 직렬 장치나 I2C 주소가 없어 실제 센서 응답
시험은 수행하지 못했다.

## 전체 빌드 결과

| 구성요소 | 결과 |
|---|---|
| ROS 2 Jazzy Desktop | 설치 성공 |
| Raspberry Pi libcamera | 빌드 성공 |
| camera_ros | 빌드 및 IMX708 실제 테스트 성공 |
| sllidar_ros2 | 빌드 성공, 하드웨어 미연결 |
| MLX90640 C++ | ARM64 빌드 성공, 센서 미감지 |
| MLX90640 Python | ARM64 공유 라이브러리 빌드 성공 |
| DFRobot C4001 | Python 문법과 모듈 검증 성공, 센서 미감지 |

## 재부팅 후 현재 Codex 세션 이어가기

터미널을 열고 다음 명령을 실행하면 이 프로젝트에서 가장 최근 Codex
세션을 바로 이어간다.

```bash
codex resume --last -C /home/gosunwoo/fire_robot_rpi
```

세션 목록에서 직접 골라서 열고 싶으면 다음 명령을 사용한다.

```bash
codex resume --all -C /home/gosunwoo/fire_robot_rpi
```

`--last`는 가장 최근에 기록된 세션을 선택하므로, 이 세션을 이어가기
전에는 다른 새 Codex 세션을 만들지 않는 것이 좋다. 다른 세션을 먼저
사용했다면 `--all` 목록에서 이 작업 내용을 가진 세션을 선택한다.
