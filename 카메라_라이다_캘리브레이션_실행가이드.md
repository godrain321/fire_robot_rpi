# 카메라·LiDAR 캘리브레이션 실행 가이드

현재 Raspberry Pi에 연결된 **IMX708 Wide 카메라**와 **RPLIDAR A1**을 기준으로 작성했다.

## 적용된 기본 설정

- 카메라: IMX708 Wide
- 카메라 해상도: 1280×720
- 카메라 모델: fisheye
- 카메라 원본 토픽: `/camera/image_raw`
- 카메라 optical 프레임: `camera_optical_frame`
- LiDAR 장치: `/dev/ttyUSB0`
- LiDAR baudrate: 115200
- LiDAR 토픽: `/scan`
- LiDAR 프레임: `laser_frame`
- 내부 캘리브레이션 결과: `~/.ros/camera_info/camera.yaml`
- 외부 캘리브레이션 결과: `~/fire_robot_calibration/lidar_camera_extrinsic.yaml`

## 1. 최초 한 번만 설치

내부 캘리브레이션 GUI 패키지를 설치한다.

```bash
sudo apt update
sudo apt install -y ros-jazzy-camera-calibration
```

캘리브레이션 패키지는 이미 빌드되어 있다. 소스를 수정한 후 다시 빌드해야 할 때는 다음 명령을 사용한다.

```bash
cd ~/fire_robot_rpi/camera_ws
source /opt/ros/jazzy/setup.bash
colcon build --symlink-install --packages-select fire_robot_camera_calibration
```

## 2. 카메라 내부 캘리브레이션

새 터미널에서 다음 명령을 실행한다.

```bash
source /opt/ros/jazzy/setup.bash
source ~/fire_robot_rpi/camera_ws/install/setup.bash

ros2 launch fire_robot_camera_calibration intrinsic_calibration.launch.py
```

기본 체커보드 설정은 다음과 같다.

- 내부 코너 수: `8x9`
- 체커보드 한 칸 크기: `0.07m`

여기서 코너 수는 검은색·흰색 사각형 개수가 아니라 **체커보드 내부 교차점 개수**다. 실제 사용하는 보드가 다르면 반드시 값을 변경한다.

예를 들어 내부 코너가 9×6개이고 한 칸이 25mm라면 다음과 같이 실행한다.

```bash
ros2 launch fire_robot_camera_calibration intrinsic_calibration.launch.py \
  board_size:=9x6 square_size:=0.025
```

GUI가 열리면 다음 순서로 진행한다.

1. `Camera type`이 `fisheye`인지 확인한다.
2. 체커보드를 화면 중앙, 네 모서리, 가까운 위치와 먼 위치에 골고루 보여준다.
3. 체커보드를 좌우와 위아래로 기울인 영상도 수집한다.
4. 수집 조건이 채워지면 `CALIBRATE`를 누른다.
5. 보정 결과에서 직선이 자연스럽게 펴지는지 확인한다.
6. `COMMIT`을 눌러 결과를 저장한다.

기본 저장 위치:

```text
~/.ros/camera_info/camera.yaml
```

내부 캘리브레이션과 실제 로봇 운용 시 카메라 해상도는 반드시 동일해야 한다.

## 3. 카메라–LiDAR 외부 캘리브레이션

다음 조건을 먼저 만족해야 한다.

- 내부 캘리브레이션이 완료되어 있어야 한다.
- 카메라–LiDAR 마운트가 완성되어 움직이지 않아야 한다.
- LiDAR가 `/dev/ttyUSB0`에 연결되어 있어야 한다.
- 카메라와 LiDAR가 벽 모서리, 상자 또는 수직 판 같은 동일한 구조물을 볼 수 있어야 한다.

새 터미널에서 다음 명령 하나를 실행한다.

```bash
source /opt/ros/jazzy/setup.bash
source ~/fire_robot_rpi/inno_jazzy_ws/install/setup.bash
source ~/fire_robot_rpi/camera_ws/install/setup.bash

ros2 launch fire_robot_camera_calibration extrinsic_calibration.launch.py
```

이 런치는 다음 항목을 한 번에 시작한다.

- IMX708 Wide 카메라
- RPLIDAR A1
- 내부 파라미터를 사용한 영상 보정
- LiDAR 점을 영상 위에 표시하는 외부 캘리브레이션 창

### 외부 캘리브레이션 조작 키

- `w` / `s`: 카메라 x 위치 앞/뒤 조절
- `a` / `d`: 카메라 y 위치 왼쪽/오른쪽 조절
- `r` / `f`: 카메라 z 위치 위/아래 조절
- `i` / `k`: pitch 조절
- `j` / `l`: yaw 조절
- `u` / `o`: roll 조절
- `p`: 결과 저장
- `q`: 종료

영상 속 구조물의 경계와 LiDAR 점이 여러 거리에서 일치하도록 맞춘 뒤 `p`를 누른다.

외부 캘리브레이션 결과 저장 위치:

```text
~/fire_robot_calibration/lidar_camera_extrinsic.yaml
```

## 4. 외부 캘리브레이션 결과 검증

```bash
source /opt/ros/jazzy/setup.bash
source ~/fire_robot_rpi/inno_jazzy_ws/install/setup.bash
source ~/fire_robot_rpi/camera_ws/install/setup.bash

ros2 launch fire_robot_camera_calibration extrinsic_validation.launch.py
```

검증 창에서 LiDAR 점이 카메라 영상의 실제 물체 경계와 일치하는지 확인한다.

- `c`: 검증 화면 스크린샷 저장
- `q`: 종료

## 5. 마운트 완성 후 권장 작업

카메라가 LiDAR 원점에서 얼마나 앞·왼쪽·위에 있는지 자로 측정한 다음 아래 파일의 초기값에 반영한다.

```text
~/fire_robot_rpi/camera_ws/src/fire_robot_camera_calibration/config/extrinsic_initial.yaml
```

수정할 항목:

```yaml
camera_position_in_laser_frame_m:
  x_forward: 0.0
  y_left: 0.0
  z_up: 0.15
```

정확한 실측 초기값을 넣으면 외부 캘리브레이션 창에서 조절해야 하는 범위가 크게 줄어든다.

## 주의사항

- 현재 외부 캘리브레이션은 2D LiDAR 점을 영상에 수동으로 정렬하는 방식이다.
- 평면형 2D LiDAR 데이터만으로 6DoF 변환을 자동으로 유일하게 계산하기는 어렵다.
- 한 거리에서만 맞추지 말고 가까운 거리와 먼 거리에서 모두 확인한다.
- 내부 캘리브레이션 후 카메라 해상도, 렌즈 초점 또는 센서를 변경하면 다시 캘리브레이션한다.
- 외부 캘리브레이션 후 카메라나 LiDAR 마운트가 움직이면 다시 외부 캘리브레이션한다.
- 다른 RPLIDAR 모델을 사용하면 `lidar_launch_file`과 `lidar_serial_baudrate`를 해당 모델에 맞게 변경해야 한다.
